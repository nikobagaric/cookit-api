import User from "#models/user";

type UserData = {
    username: string,
    email: string,
    password: string
}
export default class UserService {
    // to add; create user with profile creation linked to it
}